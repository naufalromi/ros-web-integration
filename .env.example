# Copy this file to .env and replace both passwords before starting Compose.
DB_USER=robot_app
DB_PASSWORD=change-this-app-password
MYSQL_ROOT_PASSWORD=change-this-root-password

# Set this to the address browsers use to reach the Docker host when it is not localhost.
ROSBRIDGE_URL=ws://localhost:9090
